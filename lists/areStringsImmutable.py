string = "Hello"

string = string.replace('o', 'a')

# String values cannot be changed with indices it must use special function called 'replace()'
print(string)


# 'replace()' must be used only with string object otherwise it won't work
someStr = "World"
